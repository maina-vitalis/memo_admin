export default function RolesPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-foreground">Roles & Permissions</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        RBAC matrix management — coming next.
      </p>
    </div>
  );
}
